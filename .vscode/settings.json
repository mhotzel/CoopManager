{
    // Use pylint from the project virtualenv instead of the version bundled
    // with the extension, so the pylint_pytest plugin declared in
    // pyproject.toml can actually be loaded.
    "pylint.importStrategy": "fromEnvironment",
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
}
